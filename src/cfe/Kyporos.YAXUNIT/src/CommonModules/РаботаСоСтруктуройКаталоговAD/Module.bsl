
&Вместо("ЗаполнитьТаблицуПользователей")
Функция Мок_ЗаполнитьТаблицуПользователей(LDAPText, Рекурсивно) Экспорт
	
	ПараметрыМетода = Мокито.МассивПараметров(LDAPText, Рекурсивно);
	ПрерватьВыполнение = Ложь;
	
	Результат = МокитоПерехват.АнализВызова(РаботаСоСтруктуройКаталоговAD, "ЗаполнитьТаблицуПользователей", ПараметрыМетода, ПрерватьВыполнение);
	
	Если Не ПрерватьВыполнение Тогда
		Возврат ПродолжитьВызов(LDAPText, Рекурсивно);
	Иначе
		Возврат Результат;
	КонецЕсли;
	
КонецФункции
